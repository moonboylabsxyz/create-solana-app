export const mainContent = `
import dotenv from 'dotenv';

dotenv.config();

async function main() {
  console.log('Hello from Solana project!');
  // Your main code here
}

main().catch(console.error);
`;
